function operacoesMatematicas(x, y) {

    const soma = x + y;
    const sub = x - y;
    const mult = x * y;
    const div = x / y;

    console.log(`Soma ${soma}\n`, `Subtração ${sub}\n`, `Multiplicação ${mult}\n`, `Divisão ${div}\n`);
}

operacoesMatematicas(23,52);